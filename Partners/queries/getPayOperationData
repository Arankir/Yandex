SELECT TOP 1
    [AmountDB],
    [PriceDB],
    [VolumeDB],
    [DateOpen],
    [DateClose]
FROM [agzs].[dbo].[ARM_PayOperation] p
INNER JOIN agzs.dbo.ADAST_TRKTransaction t
    on (p.Link = t.VCode)
WHERE t.VCode = %1
