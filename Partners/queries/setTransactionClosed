UPDATE [agzs].[dbo].[ADAST_TRKTransaction]
    SET Closed = %2
WHERE VCode in (SELECT
                    Link
                FROM [agzs].[dbo].[PR_APITransaction]
                WHERE ApiId = '%1')
